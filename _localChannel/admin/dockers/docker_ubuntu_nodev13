# system ubuntu:19.10
# Node 13.3 git cron vim express etc body-parser installed 

FROM ubuntu:19.10

RUN apt update && apt autoremove
RUN apt -y install sudo curl dirmngr apt-transport-https lsb-release ca-certificates
RUN curl -sL https://deb.nodesource.com/setup_13.x | sudo -E bash -
RUN sudo apt -y install nodejs
RUN apt-get -y install git
RUN apt-get install -y cron
RUN apt-get install -y vim

RUN npm install etc
RUN npm install express
RUN npm install body-parser

ENTRYPOINT cd /var/_localChannel/ && node server.js
